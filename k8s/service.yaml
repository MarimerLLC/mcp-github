apiVersion: v1
kind: Service
metadata:
  name: mcp-github
  namespace: rockbot
  labels:
    app: mcp-github
spec:
  type: ClusterIP
  selector:
    app: mcp-github
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
